#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  *_strcpy - Entry code
  *@dest: 1st pointer of char
  *@src: 2nd pointer of char
  *
  *Return: a character
  */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
