#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  *_memset - Entry code
  *@dest: 1st char, destination
  *@src: 2nd char, source
  *@n: bytes
  *Return: a character
  */

char *_memset(char *dest, char *src, unsigned int n)
{
	return (memset(s, b, n));
}
