#include <stdio.h>
#include <string.h>
#include "main.h"

/**
  *_memcpy - Entry code
  *@dest: 1st char, destination
  *@src: 2nd char, source
  *@n: bytes
  *Return: a character
  */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (memcpy(s, b, n));
}
